const routes = {
    home: '/',
    profile: '/profile/:username',
    products: '/products',
    admin: '/admin',
    login: '/login',
    register: '/register',
    cart: '/cart'

};

export default routes;
